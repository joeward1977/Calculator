import 'package:flutter/material.dart';

// Constats for the whole GUI
const kBackgroundColor = Color(0xff000000);
const double kEdgeInset = 5;

// Constants for the first Widget (The calulator history)
const kExpressionHistoryColor = Color(0xffff5e00);
const double kExpressionHistoryFontSize = 20;

// Constants for the second Widget (The current calculation or expression)
const kExpressionColor = Color(0xffffffff);
const double kExpressionFontSize = 40;

// Constants for the third Widget (The buttons)
const double kKeypadSize = 400;
const kRowWidth = 4;
const double kButtonSize = 60;
const double kButtonTextSize = 20;
const kButtonColor = Color(0xffff5e00);
const kButtonTextColor = Colors.white;
